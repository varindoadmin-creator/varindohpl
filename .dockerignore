# Build inputs only — everything here either gets rebuilt in the image or
# must never reach it.

node_modules
.next
out
build
tmp

# Secrets: env values come from Secret Manager at deploy time, never the image
.env
.env.*
!.env.example

# Local and VCS noise
.git
.gitignore
.DS_Store
**/.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo
.claude
scripts
README.md
DEPLOY.md
Dockerfile
.dockerignore
